# Browser profiles (persistent sessions — do NOT commit)
profiles/
test_profiles/

# Python cache
__pycache__/
*.py[cod]
*.pyo

# Environment / secrets
.env
.env.*

# Logs
*.log

# Windows-only installer (not needed on Linux)
vc_redist.exe

# Camoufox downloaded browser bundle (fetched via `python -m camoufox fetch`)
# Contains large binary font/asset files — do not commit
camoufox/bundle/

# Scratch / dev files
scratch/
scratch_verify.py
